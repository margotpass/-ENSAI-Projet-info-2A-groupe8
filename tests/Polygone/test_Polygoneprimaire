import unittest
import sqlite3

class TestPolygoneprimaire(unittest.TestCase):
    def setUp(self):
        # Connect to the database
        self.conn = sqlite3.connect('pop_BDD_Factice.sql')
        self.cursor = self.conn.cursor()

    def tearDown(self):
        # Close the database connection
        self.conn.close()

    def test_something(self):
        pass

if __name__ == '__main__':
    unittest.main()


